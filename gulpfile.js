const gulp = require('gulp');

// Licenses task - placeholder for license header management
// This task completes successfully to allow the build to proceed
gulp.task('licenses', function(done) {
  // If you need to add license headers to files, you can use gulp-append-prepend here
  // For now, this is a no-op task that completes successfully
  done();
});
